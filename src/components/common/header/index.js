import React from 'react'
import styles from './styles.module'
import text from 'texts'

const Header = () => <div className={styles.container}>{text('components.header.title')}</div>

export default Header
